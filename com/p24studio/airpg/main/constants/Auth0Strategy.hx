package com.p24studio.airpg.main.constants;

import js.support.Callback;

extern class Auth0Strategy
implements js.npm.passport.Strategy
implements npm.Package.Require<"passport-auth0","*"> {
    public function new(object:Dynamic, object2:Dynamic);
}
