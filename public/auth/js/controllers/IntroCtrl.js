app.controller("IntroCtrl", function($scope, socket) {
    // Socket listeners
    // ================

    // Internal functions
    // ==================

    // Methods published to the scope
    // ==============================
});
